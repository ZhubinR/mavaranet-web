/** @type {import('next').NextConfig} */
const nextConfig = {
 
    images: {
      domains: ['back.mavaranet.net'],
    },
  
    i18n: {
      locales: ['en', 'fa'],
      defaultLocale: 'en',
    },

}



module.exports = nextConfig

