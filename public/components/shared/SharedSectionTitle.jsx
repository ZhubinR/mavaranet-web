
const SharedSectionTitle = ({title , subTitle}) => {
    return (
      <div className="shared_SectionTitle">
          <h3 className="title">{title}</h3>
          <p className="subTitle">{subTitle}</p>
      </div>
    )
  }
  
  export default SharedSectionTitle