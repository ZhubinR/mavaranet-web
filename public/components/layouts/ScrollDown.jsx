const ScrollDown = ({onClick}) => {
    return (
        <div className="scroll-down"  onClick={onClick}>
            <span></span>
            <span></span>
            <span></span>
        </div>
    )
}

export default ScrollDown